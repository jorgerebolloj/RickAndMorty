query charactersList {
  characters{
    results{
      id
      name
    }
  }
}

query characterDetail($id: ID!) {
  character(id: $id){
    name
    status
    species
    type
    gender
    image
  }
}
